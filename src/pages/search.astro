---
import Layout from '../layouts/Layout.astro';
import { getCollection } from 'astro:content';

const BASE = '/my-blog';
const query = Astro.url.searchParams.get('q') || '';

const allPosts = await getCollection('blog');
const posts = allPosts
  .filter(post => !post.data.draft)
  .sort((a, b) => b.data.published.getTime() - a.data.published.getTime());

const categoryInfo: Record<string, { name: string; icon: string }> = {
  daily: { name: '日常生活', icon: '🏠' },
  music: { name: '音乐分析', icon: '🎵' },
  books: { name: '读书鉴赏', icon: '📚' },
  ecommerce: { name: '电商运营', icon: '🛒' },
  pokemon: { name: '宝可梦', icon: '⚡' },
};

// Prepare search data
const searchData = posts.map(post => ({
  id: post.id,
  title: post.data.title,
  body: post.body.slice(0, 500),
  category: post.data.category,
  categoryName: categoryInfo[post.data.category]?.name || '未知',
  tags: post.data.tags || [],
  published: post.data.published.toISOString(),
}));
---

<Layout title={query ? `搜索: ${query} | 我的博客` : '搜索 | 我的博客'}>
  <header class="mb-12 p-8 rounded-3xl backdrop-blur-xl bg-gradient-to-br from-white/80 to-slate-100/80 dark:from-slate-800/80 dark:to-slate-900/80 border border-white/50 dark:border-slate-700/50">
    <h1 class="text-3xl font-serif font-black text-slate-900 dark:text-white mb-2">搜索</h1>
    <p class="text-slate-500 dark:text-slate-400" id="search-status">
      输入关键词搜索文章
    </p>
  </header>

  <form id="search-form" method="get" action="/my-blog/search" class="mb-8">
    <div class="relative max-w-xl">
      <input
        type="text"
        name="q"
        id="search-input"
        value={query}
        placeholder="搜索文章标题、内容或标签..."
        class="w-full h-12 pl-12 pr-4 rounded-2xl border border-slate-200 dark:border-slate-700 bg-white/80 dark:bg-slate-800/80 backdrop-blur-xl text-slate-800 dark:text-slate-100 placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-primary transition-all"
      />
      <svg class="absolute left-4 top-1/2 -translate-y-1/2 w-5 h-5 text-slate-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"/>
      </svg>
    </div>
  </form>

  <div id="search-results" class="space-y-4">
    <p class="text-center py-16 text-slate-400 dark:text-slate-500">
      输入关键词开始搜索
    </p>
  </div>

  <script type="application/json" id="search-data" set:html={JSON.stringify(searchData)} />

  <script is:inline define:vars={{ initialQuery: query }}>
    const searchIndex = JSON.parse(document.getElementById('search-data').textContent);
    const resultsContainer = document.getElementById('search-results');
    const statusEl = document.getElementById('search-status');
    const input = document.getElementById('search-input');

    function escapeHtml(text) {
      const div = document.createElement('div');
      div.textContent = text;
      return div.innerHTML;
    }

    function highlight(text, query) {
      if (!query) return text;
      const regex = new RegExp(`(${query.replace(/[.*+?^${}()|[\]\\]/g, '\\$&')})`, 'gi');
      return text.replace(regex, '<mark class="bg-primary text-primary px-1 rounded">$1</mark>');
    }

    function search(query) {
      if (!query.trim()) {
        resultsContainer.innerHTML = '<p class="text-center py-16 text-slate-400 dark:text-slate-500">输入关键词开始搜索</p>';
        statusEl.textContent = '输入关键词搜索文章';
        return;
      }

      const q = query.toLowerCase().trim();
      statusEl.textContent = `正在搜索 "${query}"...`;

      const results = searchIndex.filter(post =>
        post.title.toLowerCase().includes(q) ||
        post.body.toLowerCase().includes(q) ||
        post.tags.some(tag => tag.toLowerCase().includes(q)) ||
        post.categoryName.toLowerCase().includes(q)
      );

      statusEl.textContent = `找到 ${results.length} 条结果`;

      if (results.length === 0) {
        resultsContainer.innerHTML = `
          <div class="text-center py-16">
            <p class="text-slate-400 dark:text-slate-500 mb-4">没有找到相关文章</p>
            <p class="text-sm text-slate-400 dark:text-slate-500">试试其他关键词？</p>
          </div>
        `;
        return;
      }

      resultsContainer.innerHTML = results.map(post => {
        const title = highlight(escapeHtml(post.title), query);
        const body = highlight(escapeHtml(post.body.slice(0, 200)), query);
        const date = new Date(post.published).toLocaleDateString('zh-CN');
        const tags = post.tags.slice(0, 3).map(tag => '<span class="text-xs px-2 py-1 rounded-full bg-slate-100 dark:bg-slate-700/50 text-slate-500 dark:text-slate-400">#' + escapeHtml(tag) + '</span>').join('');
        return '<article class="group"><a href="/my-blog/post/' + post.id + '" class="block p-6 rounded-2xl backdrop-blur-xl bg-white/70 dark:bg-slate-800/50 border border-white/50 dark:border-slate-700/50 hover:bg-white dark:hover:bg-slate-800 hover:border-primary dark:hover:border-primary hover:shadow-lg transition-all duration-300"><div class="flex items-center gap-3 mb-3"><span class="text-xs text-slate-400">' + date + '</span><span class="px-2 py-0.5 text-xs bg-primary text-primary dark:bg-primary rounded-full">' + escapeHtml(post.categoryName) + '</span></div><h3 class="text-lg font-serif font-semibold text-slate-900 dark:text-white group-hover:text-primary transition-colors mb-2">' + title + '</h3><p class="text-sm text-slate-500 dark:text-slate-400 line-clamp-2">' + body + '...</p>' + (post.tags.length > 0 ? '<div class="flex gap-2 mt-3">' + tags + '</div>' : '') + '</a></article>';
      }).join('');
    }

    // Initial search
    if (initialQuery) {
      search(initialQuery);
    }

    // Live search as user types (debounced)
    let timeout;
    input.addEventListener('input', () => {
      clearTimeout(timeout);
      timeout = setTimeout(() => {
        const q = input.value.trim();
        if (q) {
          window.history.replaceState({}, '', `/my-blog/search?q=${encodeURIComponent(q)}`);
          search(q);
        }
      }, 300);
    });

    // Form submit
    document.getElementById('search-form').addEventListener('submit', (e) => {
      e.preventDefault();
      const q = input.value.trim();
      if (q) {
        window.location.href = `/my-blog/search?q=${encodeURIComponent(q)}`;
      }
    });
  </script>
</Layout>