---
// Newsletter Subscription Component
---

<div id="newsletter-modal" class="hidden fixed inset-0 z-[100] flex items-center justify-center p-4">
  <div class="absolute inset-0 bg-black/50 backdrop-blur-sm" id="newsletter-overlay"></div>
  <div class="relative rounded-3xl p-8 max-w-md w-full shadow-2xl backdrop-blur-xl bg-[rgba(30,45,65,0.95)] border border-[rgba(80,128,176,0.3)]">
    <button id="newsletter-close" class="absolute top-4 right-4 p-2 hover:bg-[rgba(80,128,176,0.2)] rounded-lg transition-colors">
      <svg class="w-5 h-5 text-[#8a9cc4]" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
      </svg>
    </button>

    <div class="text-center mb-6">
      <span class="text-4xl mb-4 block">📬</span>
      <h3 class="text-xl font-bold text-[#e2e9f5] mb-2">订阅更新</h3>
      <p class="text-sm text-[#8a9cc4]">订阅我的博客，获取最新文章推送</p>
    </div>

    <form id="newsletter-form" class="space-y-4">
      <div>
        <input
          type="email"
          id="newsletter-email"
          placeholder="your@email.com"
          required
          class="w-full px-4 py-3 rounded-xl border border-[rgba(80,128,176,0.3)] bg-[rgba(80,128,176,0.1)] text-[#e2e9f5] placeholder:text-[#7a8cb4] focus:outline-none focus:ring-2 focus:ring-[#5080b0] focus:border-transparent transition-all"
        />
      </div>
      <button
        type="submit"
        class="w-full py-3 px-4 bg-gradient-to-r from-[#5080b0] to-[#72a8d8] text-white rounded-xl font-medium hover:shadow-[0_4px 20px_rgba(80,128,176,0.4)] transition-all flex items-center justify-center gap-2"
      >
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/>
        </svg>
        订阅
      </button>
    </form>

    <div id="newsletter-success" class="hidden text-center py-4">
      <span class="text-4xl mb-4 block">✨</span>
      <p class="text-[#b2c0d8]">订阅成功！感谢你的支持 🎉</p>
    </div>

    <p class="text-xs text-[#7a8cb4] text-center mt-4">
      每周一三五更新 · 不发送垃圾邮件
    </p>
  </div>
</div>

<button
  id="newsletter-trigger"
  class="fixed bottom-24 left-4 z-50 p-3 rounded-full backdrop-blur-xl bg-[rgba(155,180,218,0.1)] border border-[rgba(80,128,176,0.3)] shadow-lg hover:shadow-[0_8px 32px_rgba(80,128,176,0.3)] transition-all hover:scale-105"
  aria-label="订阅更新"
>
  <svg class="w-5 h-5 text-[#72a8d8]" fill="none" stroke="currentColor" viewBox="0 0 24 24">
    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/>
  </svg>
</button>

<script>
  function initNewsletter() {
    const modal = document.getElementById('newsletter-modal');
    const trigger = document.getElementById('newsletter-trigger');
    const closeBtn = document.getElementById('newsletter-close');
    const overlay = document.getElementById('newsletter-overlay');
    const form = document.getElementById('newsletter-form') as HTMLFormElement;
    const success = document.getElementById('newsletter-success');
    const emailInput = document.getElementById('newsletter-email') as HTMLInputElement;

    if (!modal || !trigger) return;

    // Check if already subscribed
    const isSubscribed = localStorage.getItem('newsletter-subscribed');
    if (isSubscribed) {
      trigger.classList.add('hidden');
    }

    const openModal = () => {
      modal.classList.remove('hidden');
      emailInput?.focus();
    };

    const closeModal = () => {
      modal.classList.add('hidden');
    };

    trigger.addEventListener('click', openModal);
    closeBtn?.addEventListener('click', closeModal);
    overlay?.addEventListener('click', closeModal);

    form?.addEventListener('submit', (e) => {
      e.preventDefault();
      const email = emailInput?.value;
      if (email) {
        // Store subscription (in real app, this would send to a backend)
        const subscriptions = JSON.parse(localStorage.getItem('newsletter-emails') || '[]');
        if (!subscriptions.includes(email)) {
          subscriptions.push(email);
          localStorage.setItem('newsletter-emails', JSON.stringify(subscriptions));
        }
        localStorage.setItem('newsletter-subscribed', 'true');
        trigger.classList.add('hidden');

        // Show success
        form.classList.add('hidden');
        if (success) success.classList.remove('hidden');

        setTimeout(closeModal, 2000);
      }
    });
  }

  initNewsletter();
  document.addEventListener('astro:after-swap', initNewsletter);
</script>